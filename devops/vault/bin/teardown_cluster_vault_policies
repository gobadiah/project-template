#!/bin/zsh

SCRIPTPATH="$( cd "$(dirname "$0")" ; pwd -P )"

source $SCRIPTPATH/../../config

vault delete sys/policy/$VAULT_READ_API_POLICY
vault delete sys/policy/$CIRCLECI_POLICY
vault delete auth/token/roles/$CIRCLECI_ROLE
